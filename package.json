{
  "name": "edgemetry",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Privacy-first, self-hosted web analytics that runs entirely on Cloudflare's free tier.",
  "license": "MIT",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "seed": "node scripts/seed.mjs",
    "build:cover": "node scripts/build-cover.mjs",
    "build:demo": "node scripts/build-demo.mjs",
    "build:map": "node scripts/build-map.mjs",
    "build:fonts": "node scripts/build-fonts.mjs"
  },
  "dependencies": {
    "hono": "^4"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.8",
    "@types/node": "^26.1.2",
    "typescript": "^5",
    "vitest": "^4.1.10",
    "wrangler": "^4"
  }
}
